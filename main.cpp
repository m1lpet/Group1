/**
 * @file main.cpp
 * @brief Точка входа в программу "Система учёта рабочего времени"
 * 
 * Главная функция программы, которая устанавливает русскую локаль
 * и запускает основное меню системы.
 */

#include "system.h"
#include <locale>

/**
 * @brief Главная функция программы
 * 
 * Устанавливает русскую локаль для корректного отображения кириллицы в консоли,
 * создаёт экземпляр системы и запускает главное меню.
 * 
 * @return 0 при успешном завершении
 */
int main() {
    setlocale(LC_ALL, "Russian");
    WorkTimeSystem system;
    system.runMenu();
    return 0;
}
